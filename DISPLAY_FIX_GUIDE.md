# 🔧 Electron应用启动问题解决方案

## 📋 问题诊断

### 1. 运行诊断工具
**Windows用户:**
```bash
diagnose.bat
```

**Linux/Mac用户:**
```bash
chmod +x diagnose.sh
./diagnose.sh
```

### 2. 检查文件结构
确保以下文件存在：
- ✅ `dist/main/main.js` - 主进程文件
- ✅ `dist/preload/preload.js` - 预加载脚本  
- ✅ `dist/renderer/src/renderer/index.html` - HTML文件
- ✅ `dist/renderer/assets/main.js` - 主脚本
- ✅ `dist/renderer/assets/css/index.css` - 样式文件

## 🚀 正确的启动方法

### 方法1: 生产模式启动 (推荐)
```bash
pnpm run start
```
这会先构建应用，然后启动Electron客户端。

### 方法2: 开发模式启动
```bash
pnpm run dev
```
这会启动开发服务器和Electron，开发者工具会自动打开。

### 方法3: 使用测试脚本
```bash
node test-startup.js
```

## 🛠️ 已修复的问题

### 问题1: 主进程文件路径错误
**原问题:** `main.js`试图加载错误的HTML路径
**解决方案:** 修复为正确的路径 `../renderer/src/renderer/index.html`

### 问题2: HTML中资源路径错误  
**原问题:** 脚本和CSS文件路径不正确
**解决方案:** 修复为 `../assets/main.js` 和 `../assets/css/index.css`

## 🔍 故障排除

### 如果仍然无法显示内容：

1. **检查控制台错误**
   - 按 `F12` 打开开发者工具
   - 查看 Console 标签页的错误信息

2. **检查文件路径**
   - 确保HTML文件中的资源路径正确
   - 确认所有资源文件都能被访问

3. **重新构建应用**
   ```bash
   pnpm run build:react
   pnpm run build:electron
   ```

4. **清理缓存**
   ```bash
   rm -rf dist
   pnpm install
   pnpm run build
   ```

## 📱 界面说明

一旦应用正常启动，您将看到：
- **主界面**: 金融框架仪表板
- **左侧导航**: 市场数据、通知中心、搜索中心、设置
- **主题支持**: 自动适配明暗主题
- **实时数据**: 连接WebSocket进行实时更新

## 🎯 验证启动成功

成功启动的标志：
- ✅ Electron窗口正常显示
- ✅ 界面内容完整加载
- ✅ 可以看到左侧导航菜单
- ✅ 仪表板页面正常显示
- ✅ 开发者工具无严重错误

---

**如果按照以上步骤仍有问题，请提供具体的错误信息以便进一步排查。**
